# Agent identities — codenames stay local, never published
.agents/
.model-registry.json

# Versioned builds (local only — never committed)
builds/
_data/production/

# Local validation output
_site/
.jekyll-cache/
__pycache__/
*.py[cod]
