# Agent identities — codenames stay local, never published
.agents/
.model-registry.json

# Versioned builds (local only — never committed)
builds/
_data/production/
_site/
.jekyll-cache/
.twin-release-render/
.twin-release-clock.yml
.stage-refresh-test/
.bundle/
__pycache__/
*.py[cod]
